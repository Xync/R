all:
  hosts:
    dockerRemote:
      # Docker Remote Host
      ansible_host: "172.25.32.97"
      ansible_port: "22"
      ansible_connection: "ssh"
      ansible_user: "{{ dockerRemoteUser }}"
      ansible_password: "{{ dockerRemotePass }}"
  vars:
    dockerRemoteUser: "administrator"
    dockerRemotePass: "changeme"
