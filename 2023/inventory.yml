all:
  hosts:
    centosECOMM:
      # Docker Remote Host
      ansible_host: "172.25.32.97"
      ansible_port: "22"
      ansible_connection: "ssh"
      ansible_user: "{{ centosE-COMMUser }}"
      ansible_password: "{{ centosE-COMMPass }}"
  vars:
    centosECOMMUser: "root"
    centosECOMMPass: "changeme"
