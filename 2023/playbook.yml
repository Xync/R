- hosts: all
  tasks:
  - name: Add the user 'zonifer' with a bash shell, appending the group 'wheel' and 'sudo' to the user's groups
    ansible.builtin.user:
      name: zonifer
      shell: /bin/bash
      groups: wheel,sudo
      append: yes
